# Local PDF Extractor

Follow [AGENTS.md](AGENTS.md). Before extracting a PDF, ensure `bash install.sh` has completed. Use the local runtime only and report the JSON output paths plus any `open_issues`.
